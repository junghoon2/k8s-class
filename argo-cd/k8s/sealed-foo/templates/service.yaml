apiVersion: v1
kind: Service
metadata:
  name: {{ include "sealed-foo.fullname" . }}
  labels:
    {{- include "sealed-foo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sealed-foo.selectorLabels" . | nindent 4 }}
