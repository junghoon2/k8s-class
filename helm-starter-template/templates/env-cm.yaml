{{- if .Values.env }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "<CHARTNAME>.fullname" . }}-env
data:
{{- range $key, $val := .Values.env }}
{{ $key | indent 2 }}: {{ $val | quote }}
{{- end}}
{{- end}}

