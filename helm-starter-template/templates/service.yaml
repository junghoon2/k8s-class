apiVersion: v1
kind: Service
metadata:
  name: {{ include "atlas-default-template.fullname" . }}
  labels:
    {{- include "atlas-default-template.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "atlas-default-template.selectorLabels" . | nindent 4 }}
