{{- if .Values.envs }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "<CHARTNAME>.fullname" . }}-env
data:
{{- range $key, $val := .Values.envs }}
{{ $key | indent 2 }}: {{ $val | quote }}
{{- end}}
{{- end}}
