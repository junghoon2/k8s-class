{{- if .Values.secret}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "foo-starter.fullname" . }}-secret
data:
{{- range $key, $val := .Values.secret }}
{{ $key | indent 2 }}: {{ $val | quote }}
{{- end}}
{{- end}}
