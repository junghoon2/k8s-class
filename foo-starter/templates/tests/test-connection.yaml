apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "foo-starter.fullname" . }}-test-connection"
  labels:
    {{- include "foo-starter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "foo-starter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
