apiVersion: v1
kind: Service
metadata:
  name: {{ include "foo-helm.fullname" . }}
  labels:
    {{- include "foo-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "foo-helm.selectorLabels" . | nindent 4 }}
